#!/bin/bash
#
# Helper to run tests on Travis CI service.
#
# Will exit after first error to reduce load on Travis servers.
# On errors, developers should run full tests on their computers.
#

COMMAND=$1

# Run minimal report to reduce log size.
TRIAL_COMMAND="./bin/trial -x --reporter=text --unclean-warnings"
# Allow Ctrl+C to stop the tests and exit on first error.
# When Buffer stdout/stderr, test runner is crashing.
PYTHON_3_TEST="./admin/run-python3-tests -c"

REQUIREMENTS="\
    pyOpenSSL==0.13.1\
    service-identity==0.2\
    pycrypto==2.6.1\
    pyasn1==0.1.7\
   "

#
# Install project and dependencies with pip.
#
pip_install() {
    pip install .
    pip install $REQUIREMENTS
    # Removed compiled python files.
    find . -name "*.pyc" -or -name "*$py.class" | xargs rm -f
}


#
# Run trial on twisted inside the virtual environment.
#
run_venv_trial() {
    pip_install
    python $TRIAL_COMMAND $@ twisted
    exit_code=$?
    return $exit_code
}


COMMANDS='
    py-without-modules
    py-select-gc
    py-select
    py-poll
    py-epoll
    py-glib2
    py-3.3
    pyflakes
    documentation
    api-reference
    '


case "$COMMAND" in
    "py-without-modules")
        run_venv_trial --without-module OpenSSL --without-module Crypto
        ;;

    "py-select-gc")
        run_venv_trial --force-gc --reactor=select
        ;;

    "py-select")
        run_venv_trial --reactor=select
        ;;

    "py-poll")
        run_venv_trial --reactor=poll
        ;;

    "py-epoll")
        run_venv_trial --reactor=epoll
        ;;

    "py-glib2")
        # Install glib on the base system and run Python from the system as
        # the Python package does not support virtual environments.
        echo "Updating packages..."
        sudo apt-get update -qq
        echo "Installing dependencies..."
        sudo apt-get install -qq python-gobject
        echo "Running tests..."
        /usr/bin/python $TRIAL_COMMAND --reactor=glib2 twisted
        #run_venv_trial --reactor=glib2
        ;;

    "py-3.3")
        pip_install
        python3.3 $PYTHON_3_TEST
        exit_code=$?
        exit $exit_code
        ;;

    "pyflakes")
        pip install pyflakes==0.8.1
        pyflakes twisted
        ;;

    "twistedchecker")
        pip install pep8==1.5.1 twistedchecker==0.2.0
        twistedchecker twisted
        ;;

    "documentation")
        echo "Building narative documentation"
        pip install . sphinx==1.2.1
        python ./bin/admin/build-docs . doc/core/howto/template.
        ;;

    "api-reference")
        echo "Building api documentation"
        pip install pydoctor==0.5
        python ./bin/admin/build-apidocs . apidocs
        ;;

    *)
        echo "Unknown command $COMMAND"
        echo "Available commands: $COMMANDS"
        exit 1
esac

exit $?
